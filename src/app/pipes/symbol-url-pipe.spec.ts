import { SymbolUrlPipe } from './symbol-url-pipe';

describe('SymbolUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
