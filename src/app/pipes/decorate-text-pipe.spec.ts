import {DecorateTextPipe} from './decorate-text-pipe';

describe('DecorateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DecorateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
