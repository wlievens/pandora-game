import {FindTenurePipe} from './find-tenure-pipe';

describe('FindTenurePipe', () => {
  it('create an instance', () => {
    const pipe = new FindTenurePipe();
    expect(pipe).toBeTruthy();
  });
});
