import { GovernmentFlagUrlPipe } from './government-flag-url-pipe';

describe('GovernmentFlagUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GovernmentFlagUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
