import {PortraitUrlPipe} from './portrait-url-pipe';

describe('PortraitUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new PortraitUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
